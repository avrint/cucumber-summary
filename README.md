# Cucumber GitHub Summary Action

Generates a beautiful, interactive GitHub Actions job summary from your Cucumber JSON reports. 

Instead of digging through raw logs, this action parses your `.json` test results and creates a rich dashboard right in your GitHub Actions UI, complete with a global status, index links, and collapsible scenario steps.
